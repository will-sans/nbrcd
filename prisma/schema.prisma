generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = env("POSTGRES_URL")
}

model ActionLog {
  id           Int      @id @default(autoincrement())
  action       String
  timestamp    String
  sessionId    String
  philosopherId String
  details      Json?
}